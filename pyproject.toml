[project]
name = "autovideo-studio"
version = "0.1.0"
description = "Open-source toolkit for automating short-form video creation"
readme = "README.md"
requires-python = ">=3.11"
license = {text = "MIT"}

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
